spring:
  application:
    name: bank-account-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8090
  data:
    mongodb:
      host: localhost
      port: 27017
      database: banca
